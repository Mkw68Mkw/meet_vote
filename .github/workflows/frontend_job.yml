name: Build frontend

on:
  workflow_call:
  workflow_dispatch:

jobs:
  job_build_frontend:
    name: Build frontend code
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: my-app

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
